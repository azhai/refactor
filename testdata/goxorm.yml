kind: reverse
name: mydb
source:
   database: sqlite3
   conn_str: './testdata/test.db'
targets:
   -  type: codes
      language: golang
      output_dir: ./models
      gen_json_tag: true
      gen_table_name: false
      include_tables:
         - a
         - b
      exclude_tables:
         - c